# Process environment for ChimAnn's SITESCORE_* stages (GPU host).
name: sitescore
channels: [conda-forge, pytorch, nvidia]
dependencies:
  - python=3.10
  - pytorch>=2.6
  - pytorch-cuda=12.4
  - pip
  - pip:
      - mamba-ssm>=2.3   # provides Mamba3; needs CUDA at build time
      - -e .
